apply plugin: "war"

bootWar {
    mainClassName = "com.scnu.todo.TodoApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
